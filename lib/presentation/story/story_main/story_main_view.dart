import 'package:flutter/material.dart';

class StoryMainView extends StatelessWidget {
  const StoryMainView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
